{
  "entities": {
    "users": {
      "title": "User Profiles",
      "description": "User profiles authenticated via Firebase Auth",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "avatarColor": { "type": "string" },
        "initial": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["email", "displayName", "avatarColor", "initial"]
    },
    "chats": {
      "title": "Chat Conversations",
      "description": "Direct and group conversation channels",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "isGroup": { "type": "boolean" },
        "avatarColor": { "type": "string" },
        "avatarLetter": { "type": "string" },
        "createdBy": { "type": "string" },
        "statusText": { "type": "string" },
        "online": { "type": "boolean" },
        "createdAt": { "type": "string" },
        "lastMessage": { "type": "string" },
        "lastMessageTime": { "type": "string" },
        "lastMessageTimestamp": { "type": "number" },
        "unreadCount": { "type": "number" }
      },
      "required": ["name", "isGroup", "avatarColor", "avatarLetter", "createdBy"]
    },
    "chats/messages": {
      "title": "Chat Messages",
      "description": "Messages inside each conversation channel",
      "type": "object",
      "properties": {
        "text": { "type": "string" },
        "senderEmail": { "type": "string" },
        "senderName": { "type": "string" },
        "senderId": { "type": "string" },
        "time": { "type": "string" },
        "timestamp": { "type": "number" },
        "status": { "type": "string" }
      },
      "required": ["text", "senderEmail", "time", "timestamp"]
    }
  }
}
